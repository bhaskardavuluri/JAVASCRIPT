function add(a,b,c){               // a,b and c are three parameters 
    return a+b+c                   // this is  a return type
}
 let r1=add(10,20,30);            // r1 is the identifier which is the name 
 console.log(r1);
 let r2=add(10,20)
 console.log(r2);                 // here the output will be NaN because we didnt defined the third parameter so it becomes NaN,