function add(a,b,c=1){         // a and b are two paramters and also c=1 is  a default parameter 
console.log(a+b+c);
}
add(10,20,60)                 // 10,20,30 are three arguments 
add(10)                       // 10 is the just one arguments we didnt define b and c arguments so the output will be the NaN.