function add(a,b){                // a and b are two parameters
    console.log(a+b);

}
add("rahul",25)                   // we have one argument as a string and one as a number so it performs concatenation.