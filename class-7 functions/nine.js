function add(a,b,c){                        // a,b and c are 3 parameters we have taken 
return a+b+c                                // this is an addition return type
}
let r1=add("rahul","gandhi",12)             // here we have taken 3 arguments by defining a name r1 and print them it performs concatenation
console.log(r1); 
let r2=add("rahul",20)                      // here we have only 2 argmnts and the output for the 2 argmnts will be concatenation and for another argument can be undefined 
console.log(r2);
let r3=add("gandhi")                  // here we just defined only1 argmt the output will be same for the 1st argumeent for the remaining arguments it is undefined .
console.log(r3);